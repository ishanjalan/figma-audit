# Secrets
.env

# Node
node_modules/

# Build output
dist/
web/dist/

# Audit artifacts (per-run JSON exports — uploaded as GH Actions artifacts, not committed)
audit-*.json
# .handover-watch-state.json is intentionally committed — see handover-watch.yml

# macOS
.DS_Store
